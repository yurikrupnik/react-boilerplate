const toggleLoading = prevState => ({ loading: !prevState.loading });
const a = prevState => ({ loading: !prevState.loading });
// todo delete this, just for eslint second val

export { toggleLoading, a };
